#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_install:
	$(MAKE) DESTDIR=$$(pwd)/debian/ipset-lists prefix=/usr install

override_dh_auto_clean:
	$(MAKE) DESTDIR=$$(pwd)/debian/ipset-lists prefix=/usr uninstall
	$(MAKE) DESTDIR=$$(pwd)/debian/ipset-lists prefix=/usr clean

