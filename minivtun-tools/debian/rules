#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_install:
	$(MAKE) DESTDIR=$$(pwd)/debian/minivtun-tools prefix=/usr install

override_dh_auto_clean:
	$(MAKE) DESTDIR=$$(pwd)/debian/minivtun-tools prefix=/usr uninstall
	$(MAKE) DESTDIR=$$(pwd)/debian/minivtun-tools prefix=/usr clean

